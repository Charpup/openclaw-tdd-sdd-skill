{#
Unit test template for pytest.

Variables:
- requirement: Requirement dict with id, description
- scenarios: List of scenario dicts with name, given, when, then
- module_name: Name of the module being tested
- timestamp: Generation timestamp
#}
"""
Unit tests for {{ requirement.id }}: {{ requirement.description }}

Generated: {{ timestamp }}
"""

import pytest
from {{ module_name }} import *


class Test{{ requirement.id | replace('-', '_') | title }}:
    """Tests for {{ requirement.id }}: {{ requirement.description }}"""

{% for scenario in scenarios %}
    def test_{{ scenario.name | lower | replace(' ', '_') | replace('-', '_') }}(self):
        """
        {{ scenario.name }}
        {% if scenario.given %}
        Given: {{ scenario.given }}
        {% endif %}
        {% if scenario.when %}
        When: {{ scenario.when }}
        {% endif %}
        {% if scenario.then %}
        Then: {{ scenario.then }}
        {% endif %}
        """
        # Arrange
        {% if scenario.given %}
        # {{ scenario.given }}
        {% endif %}
        # TODO: Set up preconditions
        pass

        # Act
        {% if scenario.when %}
        # {{ scenario.when }}
        {% endif %}
        # TODO: Perform action
        result = None

        # Assert
        {% if scenario.then %}
        # {{ scenario.then }}
        {% endif %}
        # TODO: Verify result
        assert result is not None  # Replace with actual assertion
        pytest.fail("Test not fully implemented")
{% else %}
    def test_placeholder(self):
        """Placeholder test for {{ requirement.id }}"""
        # TODO: Implement test based on requirement:
        # {{ requirement.description }}
        pytest.fail("Test not implemented")
{% endfor %}
