{#
Acceptance test template for pytest.

Variables:
- spec_name: Name of the specification
- scenarios: List of scenario dicts with requirement_id, name, given, when, then
- module_name: Name of the module being tested
- timestamp: Generation timestamp
#}
"""
Acceptance tests for {{ spec_name }}

Generated: {{ timestamp }}
"""

import pytest
from {{ module_name }} import *


class TestAcceptance:
    """Acceptance tests based on GIVEN-WHEN-THEN scenarios"""

{% for scenario in scenarios %}
    def test_{{ scenario.name | lower | replace(' ', '_') | replace('-', '_') }}(self):
        """
        {{ scenario.name }} ({{ scenario.requirement_id }})
        
        Given: {{ scenario.given }}
        When: {{ scenario.when }}
        Then: {{ scenario.then }}
        """
        # Arrange (Given: {{ scenario.given }})
        # TODO: Set up test data and preconditions
        pass

        # Act (When: {{ scenario.when }})
        # TODO: Perform the action being tested
        result = None

        # Assert (Then: {{ scenario.then }})
        # TODO: Verify the expected outcome
        assert result is not None
        pytest.fail("Acceptance test not fully implemented")
{% endfor %}
